# VERSION: v4.0.0 | DATE: 2024-12-19 | AUTHOR: VeloHub Development Team
# Configurações do Backend - Console de Conteúdo VeloHub
#
# IMPORTANTE:
# - Variáveis sensíveis (MongoDB URI, JWT Secret, API Keys) são gerenciadas via Secret Manager do GCP
# - Este arquivo contém apenas variáveis não sensíveis para referência
# - Secrets são mapeados como variáveis de ambiente no Cloud Run

# Porta do servidor
PORT=3001

# Ambiente (development, production)
NODE_ENV=production

# CORS Configuration
# Atualizar com a URL do Cloud Run após deploy
CORS_ORIGIN=https://frontend-console-[HASH]-us-east1.run.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# VARIÁVEIS GERENCIADAS VIA SECRET MANAGER
# ===========================================
# As seguintes variáveis são gerenciadas via Secret Manager do GCP:
# - MONGODB_URI (secret: MONGO_ENV)
# - JWT_SECRET (se usado)
# - API_KEY (se usado)
# - OPENAI_API_KEY (secret: OPENAI_API_KEY)
# - GOOGLE_CLIENT_SECRET (secret: google-client-secret)
#
# Para configurar, acesse: Google Cloud Console → Secret Manager → Cloud Run
# ===========================================