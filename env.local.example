# VERSION: v2.0.0 | DATE: 2024-12-19 | AUTHOR: VeloHub Development Team
# Configurações para Desenvolvimento Local - Console de Conteúdo VeloHub
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Configure as variáveis conforme necessário
# 3. O arquivo .env está no .gitignore e NÃO será commitado
#
# IMPORTANTE:
# - Variáveis sensíveis (Google Client ID, MongoDB URI, etc.) são gerenciadas via Cloud Run (produção)
# - Para desenvolvimento local, configure apenas as variáveis abaixo
# - Secrets do backend são gerenciados via Secret Manager do GCP

# ===========================================
# CONFIGURAÇÕES DO FRONTEND (REACT)
# ===========================================

# URL da API Backend (Produção)
REACT_APP_API_URL=https://backend-gcp-278491073220.us-east1.run.app/api

# Modo de Desenvolvimento
REACT_APP_DEV_MODE=true

# Google OAuth Client ID (para desenvolvimento local)
# Em produção, esta variável é configurada via variável do container "google-client-id" no Cloud Run
# REACT_APP_GOOGLE_CLIENT_ID=seu_google_client_id_aqui

# Domínio autorizado para login (para desenvolvimento local)
# Em produção, esta variável é configurada via variável do container "authorized-domain" no Cloud Run
# REACT_APP_AUTHORIZED_DOMAIN=velotax.com.br

# ===========================================
# CONFIGURAÇÕES DO BACKEND (se rodando localmente)
# ===========================================

# Porta do servidor backend local (se aplicável)
PORT=3001

# Ambiente
NODE_ENV=development

# CORS para desenvolvimento local
CORS_ORIGIN=http://localhost:3000

# ===========================================
# CONFIGURAÇÕES DE DESENVOLVIMENTO
# ===========================================

# Debug mode
DEBUG=true

# Logs detalhados
VERBOSE_LOGS=true

# ===========================================
# INSTRUÇÕES DE USO
# ===========================================
# 
# 1. Este arquivo está configurado para usar a API de produção
# 2. Para desenvolvimento local, acesse: http://localhost:3000
# 3. A API será chamada em: https://backend-gcp-278491073220.us-east1.run.app/api
# 4. Variáveis sensíveis (MongoDB, JWT, API Keys) são gerenciadas via Secret Manager no Cloud Run
# 5. Este arquivo está no .gitignore e NÃO será commitado
#
# ===========================================
